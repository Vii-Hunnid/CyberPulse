# /project:scan

Run a full TypeScript check and lint pass, then report any issues.

```bash
npx tsc --noEmit && npm run lint
```

If there are errors, fix them one by one. Report a summary of what was fixed.
